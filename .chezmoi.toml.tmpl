{{- $peacockColor := "#efceb7" -}}
{{- $devcontainer := (env "REMOTE_CONTAINERS") -}}
{{ if not $devcontainer }}
{{ $devcontainer = "false" }}
{{- end -}}

[data]
devcontainer = {{ $devcontainer | quote }}
{{- if $devcontainer }}
peacockColor =  "#00bfff"
{{- else }}
peacockColor = {{ $peacockColor }}
{{- end }}

[edit]
command = "code"
args = ["--wait"]

